package com.k_int.accesscontrol.grails

/**
 * A trait to track all resources controlled by policies.
 * Implementing classes will automatically have their associated policies
 * removed when the resource itself is deleted.
 */
trait PolicyControlled {

}