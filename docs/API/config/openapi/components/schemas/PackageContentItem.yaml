type: object
required:
  - class
allOf:
  - $ref: ./ErmResource.yaml
  - properties:
      class:
        enum:
          - org.olf.kb.PackageContentItem
      accessStart:
        type: string
        format: date
      accessEnd:
        type: string
        format: date
      addedTimestamp:
        type: integer
      depth:
        type: string
      lastSeenTimestamp:
        type: integer
      longName:
        type: string
      note:
        type: string
      removedTimestamp:
        type: integer
      embargo:
        $ref: ./Embargo.yaml
      pkg:
        $ref: ./Package.yaml
      pti:
        $ref: ./PlatformTitleInstance.yaml
